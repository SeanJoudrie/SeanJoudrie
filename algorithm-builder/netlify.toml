[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# The app calls /api/search; Netlify serves it from the function.
[[redirects]]
  from = "/api/search"
  to = "/.netlify/functions/search"
  status = 200
