/* Account sync settings. Leave both empty to keep projects in the browser only.
   Both values are public by design (Supabase's publishable key); the database
   is protected by row level security, not by hiding these. */
window.LC_CONFIG = {
  supabaseUrl: '',
  supabaseKey: ''
};
